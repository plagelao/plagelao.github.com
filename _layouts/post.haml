---
layout: default
---

%article{class: "{{ page.type}}" }
  %h2 {{ page.title }}
  .content
    {{ content }}
  .footer
    %p.meta-data
      %a{href: "/{{page.type}}"} {{ page.human_type }}
      \|
      {{ page.date | date: "%d %B %Y" }}
      \|
      {{page.estimated_time}}
